{
  "verbose": true,
  "template": {
    "data": "./data/penn",
    "model": "LSTM",
    "emsize": [50, 100],
    "nhid": [50, 100],
    "nlayers": 1,
    "lr": 20,
    "clip": 0.5,
    "epochs": 6,
    "batch_size": 20,
    "vocab_size": 8000,
    "sequence_length": 20,
    "seed": 1111,
    "log_interval": 200,
    "logfile": "logs/{}.txt",
    "results": "results/{}.txt",
    "save": "models/{}.pt"
  },
  "max_saved_models": 50,
  "training_cap": 100
}
